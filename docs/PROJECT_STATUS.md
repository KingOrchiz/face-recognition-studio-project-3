# Face Recognition Studio — verified project status

Last updated: 2026-09-06 06:09 WAT

| Area | Exact status | Evidence boundary |
|---|---|---|
| Source/build | Current C++17/OpenCV application configures and builds on Linux x86-64; a preserved earlier source baseline configured and built on Apple-silicon macOS | baseline macOS run verified AppleClang 21 and Homebrew OpenCV 4.14.0; later source changes have static stock-macOS compatibility evidence but still need a refreshed runtime build |
| Default detector | The executable now receives the same version-aware YuNet selection already used by CTest: 2022 for OpenCV before 4.10 and 2023 for 4.10+; `--build-info` exposes the compiled choice and the packaged audit requires the chosen file to exist | Linux/OpenCV 4.6 default selection verified as the bundled 2022 graph; refreshed AppleClang/OpenCV 4.14 execution remains pending while the paired Mac is offline |
| Automated tests | The same nine named checks passed 9/9 on Linux (2.94 seconds) and Apple-silicon macOS (2.08 seconds) | timings are bounded single runs, not benchmarks |
| Compiler diagnostics | An opt-in `FACE_STUDIO_WARNINGS_AS_ERRORS` CMake switch applies `-Werror` on top of the existing `-Wall -Wextra -Wpedantic` project diagnostics; a clean GNU C++ 13.3.0 configure/compile/link and 9/9 regression passed; the dedicated Mac setup/build path now enables the switch automatically | Linux strict validation and camera-free Mac rerun preparation only; the switch has not yet been exercised with AppleClang because the paired Mac is offline |
| Still-image detection | Haar fallback detected 1 face on the original 1254×1254 synthetic portrait; YuNet 2022 detected 1 face on the 640×640 derivative | YuNet 2023 is incompatible with OpenCV 4.6.0; YuNet 2022 gave duplicate boxes at 1254×1254 |
| Video pipeline | 3.0-second 640×640 10 fps synthetic input processed 30/30 frames; verified output is 30 frames and 3.0 seconds | measured 1.48-second Linux run is not a general performance claim |
| Mode-safe output defaults | Image mode defaults to `output/result.jpg`; video and camera modes default to `output/result.mp4`. The existing 30-frame video CTest now omits `--output`, requires the MP4 destination in program output, and produced a non-empty 105,465-byte file | verified with synthetic video on Linux; the paired Mac was offline, so the changed source has not received a refreshed Mac runtime rerun |
| Video output safety | Video mode rejects an empty output unless `--display` is enabled, rejects non-MP4 output filenames, refuses unrelated existing outputs by default, and rejects identical, relative-alias, hard-link, and symbolic-link input/output paths before model initialization or writer creation; explicit `--overwrite` accepts only a regular destination with exactly one hard link, while symbolic links, directories, and multiply linked files are refused and preserved | eleven camera-free Linux cases cover observability, format, default evidence preservation, bounded explicit opt-in, three unsafe destination types, and four source-path controls; broader filesystem and codec failure modes are outside this bounded check |
| Image output safety | Image mode refuses unrelated existing outputs by default and rejects identical, relative-alias, hard-link, and symbolic-link input/output paths before model initialization; explicit `--overwrite` accepts only a regular destination with exactly one hard link, while symbolic links, directories, and multiply linked files are refused and preserved | nine camera-free Linux cases cover default evidence preservation, bounded explicit opt-in, three unsafe destination types, and four filesystem-equivalence forms; broader filesystem failure modes are outside this bounded check |
| CLI numeric validation | Threshold text must be fully numeric and finite, and a supplied camera index must fit a non-negative integer; trailing-text, non-number, NaN, infinity, negative-index, and overflow controls were rejected 7/7 with status 2 before engine or device work | camera-free parser regression only; no camera device was opened or enumerated |
| Recognition | Synthetic_A query scored 0.934496; different synthetic identity scored 0.391773. At threshold 0.400 the decisions were Synthetic_A and `Unknown` | two-query synthetic regression only; no accuracy/fairness claim |
| Release package | Reproducible archive created; `dist/` is now fail-closed to the canonical archive plus its adjacent SHA-256 handoff file, clean-extraction hashes and current-workspace freshness are enforced, pre-extraction metadata is bounded and restricted to exact path-specific permission roles, identical consecutive builds are verified, and the extracted source passed safe/unique embedded-manifest paths, exact manifest/file-set, symlink, generated/private-directory, credential-like-filename, text-privacy, local-documentation-link/anchor, all-shell-script portability/syntax, independently pinned ONNX model, version-aware default-detector selection, bounded default-threshold selection, seven-post readiness, final-report 6/6 working-draft contract, 17/17 key evidence-claim traceability, README-to-caption-count consistency, selected-PNG integrity and metadata privacy, semantic camera-refusal, camera-output preservation, camera-preflight contract, image/video source-overwrite refusal, configure/compile/link, and 9/9 test checks | the Mac portability gate proves exact inventory coverage of all 33 shell nodes reachable from the release validator, scans its 32 executable dependencies, and rejects 28 Bash 4-only or GNU/non-stock command forms plus 2/2 synthetic coverage gaps; selected screenshots must contain only IHDR/contiguous IDAT/IEND chunks, valid CRCs, and no trailing payload; the stale 14:27 Mac bundle was detected, rejected by the new inventory control, and moved to recoverable workspace trash so it cannot be mistaken for the current handoff; full consumer validation ran on Linux through the forced macOS-compatible `shasum` branch, including from a handoff folder containing spaces; the paired Mac remains offline, so a refreshed runtime rerun is not claimed |
| Default threshold | The application now defaults to the bounded 0.400 setting, where the synthetic known query remained `Synthetic_A` and the different identity became `Unknown`; OpenCV's 0.363 example value falsely accepted the different synthetic identity | two-query regression only; negative 0.363 result is preserved and broader calibration remains necessary |
| macOS | User-executed validation configured, compiled and linked successfully; CTest passed 9/9 in 2.08 seconds. The repeatable Mac helper now records exact source/helper SHA-256 identities, uses the compiled version-aware YuNet default, fails on Haar fallback or incorrect known/unknown labels, and creates timestamped outputs rather than colliding with evidence-preservation controls | terminal result preserved and a clearly labelled privacy-clean derived evidence screenshot archived; the corrected helper passed its source-identity contract, syntax, stock-Bash compatibility, and equivalent Linux functional checks, but its refreshed AppleClang/OpenCV 4.14 execution remains pending while the paired Mac is offline |
| Camera | Not accessed; camera mode exits with status 2 unless `--confirm-camera` is supplied. Even after confirmation, the executable rejects `--overwrite`, an empty output without `--display`, non-MP4 filenames, existing regular files, directories, dangling symbolic links, missing parent directories, and non-directory parents as camera outputs before model/device work; its camera-free 9/9 audit also proves the unused-path control reaches only the deliberately missing-model gate. A camera-safe preflight verifies the executable, all three pinned models, unused MP4 target, exact refusal, early gate ordering, and absent output without enumerating a device. Its repeatable contract audit passes 6/6 across missing-executable, non-MP4, missing-directory, existing-target, dangling-symbolic-link, and safe-success cases while preserving both existing target forms. The consented-test runbook and evidence-record gate enforce authorization, scene, result, retention, screenshot-basename, privacy-pattern, and Evidence → Milestone → Lesson → Next step completeness before Post 5 can become a webcam-result claim. A separate camera-free 5/5 contract audit accepts both valid record states and rejects failed retention, private paths, and missing narrative fields | hardware operation still requires Oche's explicit camera-test confirmation at test time; completed-record fixtures are synthetic validator inputs, not webcam evidence |
| Yellowdig | All seven drafts pass automated structure, exact per-post screenshot-set mapping, and 17/17 key-claim traceability audits; stale or extra references are rejected across the drafts, publication control, and caption guide; all 23 exact draft attachments have privacy-scanned, copy-ready alt text, and the README's documented total is automatically cross-checked; a selected-folder disposition gate classifies all 54 PNGs as 23 attachments, 25 review aids, 3 optional supporting items, or 3 superseded records, and a disposable unclassified PNG is rejected; exact folder-to-register parity is enforced for all 54 selected screenshots, and register IDs must be unique and contiguous from E01; an 8/8 fail-closed handoff-status audit rejects stale totals, stale evidence-sequence summaries, and a false current-source Mac claim in the runbook; the submission-readiness matrix passes a 5/5 state/owner/narrative contract; the future published-capture folder passes a 7/7 intake contract covering honest empty state, contiguous sequence, filenames/dates, regular files, and complete PNGs | Review aids, optional evidence, superseded captures, unindexed screenshots, malformed evidence IDs, stale handoff summaries, Mac baseline/current-source overclaims, and malformed or out-of-sequence publication records cannot enter the handoff unnoticed; Post 5 still needs consented webcam evidence; Posts 6–7 are held behind Post 5; nothing published by Jane |

## Immediate next actions

1. Use `docs/SUBMISSION_READINESS.md`, `docs/YELLOWDIG_PUBLICATION_CONTROL.md`, and the selected review cards for Oche's final content/privacy/accessibility handoff; keep Post 4 held until the Post 3 cadence gate is cleared.
2. Obtain Oche's explicit camera-test confirmation before any Post 5 camera command or permission request.
   Once confirmed, follow `docs/CAMERA_TEST_RUNBOOK.md` and stop on any listed
   privacy, device, output-path, or preview-control abort condition.
3. Keep Post 6 held until Post 5 is resolved in sequence.
4. After the camera gate is truthfully resolved or retained as a limitation, finalize the Post 7 wording and Oche review pack; its cross-platform regression/package/contact-sheet evidence is ready.
5. After each real post, place one `post-0N-YYYY-MM-DD.png` capture in `evidence/yellowdig-published/`, run the intake audit, manually review privacy/content, and index the accepted file.
6. Complete the explicit operator fields in `docs/FINAL_LAB_REPORT_DRAFT.md`; do not export a final PDF until real GitHub and published-post evidence is available.
